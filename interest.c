#include <stdio.h>

int main()
    {
        int a, b;
        printf("Enter values a and b");
        scanf("%d%d", &a, &b);
        c= a+b;
        printf("sum", a,b c);
        return 0;
    }
